SSG		SEGMENT PARA STACK 'STACK'
		DW 200 DUP(?)
SSG		ENDS

DSG		SEGMENT PARA 'DATA'
n		DW 10
DSG		ENDS

CSG		SEGMENT PARA 'CODE'
		ASSUME	CS:CSG, SS:SSG, DS:DSG
		
DNUM		PROC FAR
		PUSH BP
		PUSH AX
		MOV BP, SP
		MOV AX, [BP + 8]
		CMP AX, 0
		JG ZR
		MOV WORD PTR [BP + 8], 0
		POP AX
		POP BP
		RETF
ZR:		CMP AX, 2
		JG TW
		MOV WORD PTR [BP + 8], 1
		POP AX
		POP BP
		RETF
TW:		PUSH BX
		PUSH DX
		MOV BX, AX
		DEC BX
		PUSH BX
		CALL DNUM
		CALL DNUM
		POP DX
		PUSH DX
		PUSH AX
		SUB AX, 2
		PUSH AX 
		CALL DNUM
		POP DX
		POP AX
		DEC AX
		SUB AX, DX
		PUSH AX
		CALL DNUM
		POP BX
		POP DX
		ADD DX, BX
		MOV [BP + 8], DX
		POP DX
		POP BX
		POP AX
		POP BP
		RETF
DNUM		ENDP

PRINTINT	PROC NEAR
		PUSH BX
		PUSH BP
		PUSH DX
		MOV BP, SP
		MOV AX, [BP + 8]
		MOV BX, 1
		PUSH BX
		XOR DX, DX
		MOV BX, 0Ah	
P1:		DIV BX
		ADD DX, '0'
		PUSH DX
		XOR DX, DX
		CMP AX, 0
		JNZ P1
P2:		POP AX
		CMP AX, 1
		JZ P3
		MOV DL, AL
		MOV AH, 2
		INT 21h
		JMP P2
P3:		POP DX
		POP BP
		POP BX
		RET 2
PRINTINT	ENDP

MN		PROC FAR
		PUSH DS
		XOR DX, DX
		PUSH DX
		MOV DX, DSG
		MOV DS, DX		
		MOV DX, n
		PUSH DX
		CALL DNUM
		CALL PRINTINT
		RETF
MN		ENDP
CSG		ENDS
		END	MN
